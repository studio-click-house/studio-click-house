# Local Workspace Agent Config

This folder contains all of the custom AI skills and MCP servers we downloaded for this project.

The `skills.json` file explicitly registers the `skills/` directory so the AI will always look here first to load our specific instructions (like GSAP animations, Shadcn UI generation, and Framer Motion logic) before relying on its generic defaults.
